<%- include('partials/header.ejs') %>

  <body>
    <%- include('partials/navbar.ejs') %>
      <div class="container">
        <hr>
        <h1>Perguntas</h1>
        <a href="/perguntar" class="btn btn-primary
        ">Faça sua Pergunta</a>
        <hr>
        <br>
        <% Pergunta.forEach(Pergunta=> { %>
          <br>
          <div class="card">
            <div class="card-body">
              <h3>
                <%=Pergunta.titulo %>
              </h3>
            </div>
            <div class="card-footer">
              <a href="/pergunta/<%=Pergunta.id %>"><button class="btn btn-primary">
                  responder
                </button></a>
            </div>
          </div>

          <% })%>
      </div>
  </body>
  <%- include('partials/footer.ejs') %>